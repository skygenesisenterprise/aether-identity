# Configuration de l'environnement pour le serveur Aether Identity

# JWT Configuration
JWT_SECRET=votre-cle-secrete-ici
ACCESS_TOKEN_EXP=15
REFRESH_TOKEN_EXP=720

# Database Configuration
DATABASE_URL=host=localhost user=postgres password=postgres dbname=aether_identity port=5432 sslmode=disable

# Server Configuration
PORT=8080

# System Key - Clé système pour les requêtes internes de l'application
# Cette clé est utilisée pour authentifier les requêtes depuis localhost
SYSTEM_KEY=sk_system_default_key_change_in_production

# CORS Configuration - Origines autorisées (séparées par des virgules)
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8080,http://127.0.0.1:3000

# Default Redirect Paths - Chemins par défaut après login/logout
DEFAULT_POST_LOGIN_PATH=/
DEFAULT_POST_LOGOUT_PATH=/
